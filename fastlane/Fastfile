default_platform(:ios)

platform :ios do

  before_all do
    carthage(platform: 'ios')
  end

  desc "setup library"
  lane :setup_library do 
    carthage
  end

  desc "exec test"
  lane :test do
    run_tests
  end
end
